# Frontend

The frontend is based on a locked design source and a separate logic file.

Important files:

- `Mon Coffre - Application.html`: locked design source;
- `mc_logic.js`: application logic and backend adapter;
- `build_mc.py`: rebuild script.

Do not edit the compiled design manually. Update the logic layer and rebuild instead.

